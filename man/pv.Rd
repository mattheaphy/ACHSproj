% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pv.R
\name{pv}
\alias{pv}
\title{Present value of a cashflow object}
\usage{
pv(x, disc, disc_freq = 1L, rolling = FALSE, ...)
}
\arguments{
\item{x}{A \code{cf_vec} or numeric vector of cashflow values.}

\item{disc}{Discount rate as a numeric vector of length 1 or the same
length as \code{x}. A scalar is recycled across all periods.}

\item{disc_freq}{Periodicity of the discount rate as number of periods per
year: \code{12} (monthly), \code{6} (bimonthly), \code{4} (quarterly), \code{3} (trimonthly),
\code{2} (semi-annual), or \code{1} (annual, default).}

\item{rolling}{If \code{TRUE}, returns a numeric vector of length \code{length(x)}
where element \code{i} is the prospective reserve at time \code{i}: the present
value, discounted to time \code{i}, of cashflows \code{i+1} through \code{n}. The last
element is always zero. Default \code{FALSE} returns a scalar.}

\item{...}{Additional arguments passed to methods. For numeric vectors,
\code{freq} sets the cashflow periodicity as number of periods per year:
\code{12} (monthly, default), \code{6} (bimonthly), \code{4} (quarterly), \code{3} (trimonthly),
\code{2} (semi-annual), or \code{1} (annual).}
}
\value{
When \code{rolling = FALSE}, a scalar double. When \code{rolling = TRUE}, a
numeric vector of length \code{length(x)}.
}
\description{
Present value of a cashflow object
}
