% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irr.R
\name{irr}
\alias{irr}
\title{Internal rate of return of a cashflow vector}
\usage{
irr(x, interval = c(0, 0.25), disc_freq = 1L, tol = 1e-16, maxiter = 50L, ...)
}
\arguments{
\item{x}{A \code{cf_vec} or numeric vector of cashflow values.}

\item{interval}{Numeric vector of length 2 giving the lower and upper bounds
of the search interval for the rate. Default \code{c(0, 0.25)}.}

\item{disc_freq}{Periodicity of the returned rate as number of periods per
year: \code{12} (monthly), \code{6} (bimonthly), \code{4} (quarterly), \code{3} (trimonthly),
\code{2} (semi-annual), or \code{1} (annual, default).}

\item{tol}{Convergence tolerance passed to \code{\link[=uniroot]{uniroot()}}. Default \code{1e-16}.}

\item{maxiter}{Maximum number of iterations passed to \code{\link[=uniroot]{uniroot()}}. Default \code{50}.}

\item{...}{Additional arguments passed to methods. For numeric vectors,
\code{freq} sets the cashflow periodicity as number of periods per year:
\code{12} (monthly, default), \code{6} (bimonthly), \code{4} (quarterly), \code{3} (trimonthly),
\code{2} (semi-annual), or \code{1} (annual).}
}
\value{
A scalar double — the internal rate of return as an effective rate
at \code{disc_freq} frequency (e.g. an annual effective rate when
\code{disc_freq = 1L}).
}
\description{
Finds the discount rate at which the present value of \code{x} is zero, using
\code{\link[=pv]{pv()}} as the objective function.
}
