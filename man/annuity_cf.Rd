% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annuity_cf.R
\name{annuity_cf}
\alias{annuity_cf}
\title{Project payout annuity cashflows and reserves}
\usage{
annuity_cf(
  issue_age,
  gender = c("Female", "Male"),
  benefit = 1,
  premium = NULL,
  qx_scalar = 1,
  disc,
  defer_years = 0L,
  certain_years = 0L,
  life_contingent = TRUE,
  refund = FALSE,
  start_date = Sys.Date()
)
}
\arguments{
\item{issue_age}{Integer age at policy issue.}

\item{gender}{\code{"Female"} or \code{"Male"}.}

\item{benefit}{Annual benefit amount. Default \code{1}.}

\item{premium}{Single premium paid at time 0. If \code{NULL} (default), the
premium is set to the present value of benefits — the net-zero fair
premium at rate \code{disc}. Incompatible with \code{refund = TRUE}; supply an
explicit value in that case.}

\item{qx_scalar}{Scalar multiplier applied to base mortality rates before
improvement. Values below 1 lighten mortality; values above 1 load it.
The adjusted rate is capped at 1. Default \code{1} (no adjustment).}

\item{disc}{Annual discount rate. Passed to \code{\link[=pv]{pv()}}.}

\item{defer_years}{Number of years before payments start. Default \code{0}.}

\item{certain_years}{Number of years where payments are guaranteed regardless
of survivorship. Default \code{0}.}

\item{life_contingent}{If \code{TRUE} (default), life-contingent payments are
assumed. If \code{FALSE}, a certain annuity is assumed.}

\item{refund}{If \code{TRUE}, upon death a benefit is paid equal to the premium
less cumulative undecremented past payments. Default \code{FALSE}.}

\item{start_date}{Projection start date. Parsed via \code{\link[lubridate:ymd]{lubridate::ymd()}}.}
}
\value{
A \link{projection} object with the following columns appended to \verb{@data}:
\itemize{
\item \code{pol_yr}: Policy year.
\item \code{age}: Integer attained age.
\item \code{qx}: Base annual mortality rate from \link{qx_iamb}.
\item \code{mi}: Annual improvement factor from \link{scale_g2}.
\item \code{qx_adj}: Mortality rate after applying \code{qx_scalar} and Scale G2
improvement from 2012 (the Scale G2 base year). Capped at 1.
\item \code{tpx}: Cumulative survival probability from time 0.
\item \code{premium}: Single premium paid at time 0; zero thereafter.
\item \code{certain_cf}: Certain benefit cashflows as a \link{cf_vec}. Non-zero only
during the certain period; scaled by survival probability to the end of
the deferral period.
\item \code{life_cf}: Life-contingent benefit cashflows as a \link{cf_vec}. Zero during
the deferral and certain periods.
\item \code{hist_pay}: Cumulative undecremented benefit payments to date.
\item \code{death_cf}: Refund death benefit cashflows as a \link{cf_vec}. Non-zero only
when \code{refund = TRUE} and unrecovered premium remains.
\item \code{total_cf}: Sum of all cashflows as a \link{cf_vec}.
\item \code{disc}: Discount rate.
\item \code{reserve}: PV of future total cashflows at each period.
}
}
\description{
Appends mortality, survival, cashflow, and reserve columns to a
\link{projection} object's \code{data} tibble.
}
